package control;

import entity.Estado;
import entity.Objeto;
import entity.ObjetoEstado;
import entity.Objeto_;
import entity.Ruta;
import entity.TipoObjeto;
import java.math.BigDecimal;
import java.util.Date;
import java.util.List;
import javax.swing.JOptionPane;
import persistencia.ControlPersistencia;

public class TestJPA {

    public static void main(String[] args) {
        //ControlPersistencia control = new ControlPersistencia();
        /*
        //Estado estado = new Estado(Integer.SIZE, "Creado desde", new Date("03/07/2022"),"aaa");
        //Estado estado3 = new Estado(Integer.SIZE, "Prueba", new Date("03/07/2022"),"Algunas");
        //control.crearEStado(estado3);
        //control.eliminarEstado("1");
        //estado3.setNombre("Prueba2");
        //estado3.setObservaciones("Activo");
        //control.modificarEStado(estado3);
        
        List <Estado> listaEstado  = control.traerAlumnos();
        System.out.println("\nListado Base de datos\n");
        for(Estado est : listaEstado){
            System.out.println(" * " + est.getIdEstado() + " " + est.getNombre() + " " + est.getFechaCreacion() + " " + est.getObservaciones());
        }
        control.eliminarEstado(8);

         */
       
        

    }

}
